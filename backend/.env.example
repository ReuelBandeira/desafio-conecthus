# Application
BACKEND_PORT=4050
DOCKER_PORT=5000
BCRYPT_ROUNDS=10

# Database (docker-compose MySQL container)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=conecthus_user

# Database (used by the app to connect — same credentials as above)
DB_HOST=localhost
DB_PORT=3307
DB_USERNAME=root
DB_PASSWORD=root
DB_DATABASE=conecthus_user