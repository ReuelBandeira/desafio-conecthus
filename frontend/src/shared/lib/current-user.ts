// No login flow in scope (see PDF spec — only the CRUD screens are
// required), so the "logged in" user is a fixed placeholder matching the
// design reference, not a real session.
export const CURRENT_USER_NAME = 'Millena Santana Borges';
export const CURRENT_USER_EMAIL = 'milena.santana@mpq.org.br';
